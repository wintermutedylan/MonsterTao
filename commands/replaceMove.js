// use this to replac ea move with another move
// use addmove code to check for if the pokemon can use the move
// then just set the move array to the new mvoe array instead of pushign
// make sure to replace the old move with the new move in the old move array before setting to actually replace the move